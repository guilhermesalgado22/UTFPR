import 'package:flutter/material.dart';

class Satelite extends StatelessWidget {
  Satelite({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Maps'.toString()),
      ),
    );
  }
}
